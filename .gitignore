# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# ─── Bảo mật: cấu hình & bí mật ────────────────────────────
# .env: file cấu hình chứa API key / client id thật (KHÔNG BAO GIỜ commit)
.env
.env.local
.env.*.local
# Bất kỳ biến thể nào của .env trừ .env.example (bản mẫu trống, an toàn)
!.env.example

# config.json: chứa API key / cấu hình môi trường cục bộ
config.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?